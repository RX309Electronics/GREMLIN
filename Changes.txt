Patched Services.sh and made it so the output of App_init.sh goes to the console but also logfile
Added App_init.sh placeholder
Patched build.sh to not clear the console when an build error occurs.
Patched python3 package to enable tkinter
Added logging to build.sh
added openGL
Added tweaks step to prepare.sh
modified the Test.sh script to enable to boot either the ramdisk or disk.img
Added functionality to be able to boot from not only an initrd but also a ext2/ext4 filesystem
removed pbscript cause its not needed and was causing issues
modified buildroot.config
added S03network-online service that waits till the network is online
modified Services.sh to include a shutdown section
modified build.sh yet again, to include the actions the previous pbscript had
Added some filesystem utiltities
